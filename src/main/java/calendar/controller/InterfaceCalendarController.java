package calendar.controller;

/**
 * Interface for the calendar controller.
 */
public interface InterfaceCalendarController {
  /**
   * Starts the calendar application.
   * Processes commands in either interactive or headless mode.
   */
  void run();
}